<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style_plat.css">
    <title>Facebouf - Plats</title>
</head>
<body>
    <ul id="list-outside">
        <li>
            <ul class="list-inside"> 
                <li class="meal-titles"> ➡️ Plats </li>
                <li class="food-image"> <img src="plat_default.jpg"> </li>
                <li class="food-description"> ➡️ Description : les plats de la semaine
                    <ul>
                      <% meals[0].forEach(meal => { %>
                        <li> 
                            <%= meal.mealName %>
                            <%= meal.averageRating %>
                        </li>
                      <% }) %>
                    </ul>
                </li>
            </ul>
        </li>

        <!-- See later for adding desserts and entrées -->

        <li>
            <section id="app">
                <div class="container">
                  <div class="row">
                    <div class="col-6">
                      <div class="comment">
                    <p v-for="items in item" v-text="items"></p>
                          </div>
                        </div>
                      </div>
                  <div class="row">
                    <div class="col-6">
                  <textarea type="text" class="input" placeholder="Écrire un commentaire" v-model="newItem" @keyup.enter="addItem()"></textarea>
                      <button v-on:click="addItem()" class='primaryContained float-right' type="submit">Add Comment</button>
                    </div>
                  </div>
                </div>
              </section>
        </li>
    </ul>
    
</body>
</html>